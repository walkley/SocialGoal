@model IEnumerable<SocialGoal.Model.Models.Group>
@{
    ViewBag.Title = "MyGroups";
    Layout = "~/Views/Shared/_PageLayout.cshtml";
}
@if (Model.Count() == 0)
{
    <div class="alert alert-info">
        No results To Display
    </div>
}
@foreach (var item in Model)
{
    <div class="alert users span2">
        <h4>
           @Html.ActionLink(item.GroupName, "Index", "Group", new { id = item.GroupId },null)</h4>
        @{
            var memberCount = await Component.InvokeAsync("NoOfUsers", new { id = item.GroupId });
        }
        @Html.ActionLink(memberCount + " Members", "JoinedUsers", new { id = @item.GroupId }, null)
    </div>
}
