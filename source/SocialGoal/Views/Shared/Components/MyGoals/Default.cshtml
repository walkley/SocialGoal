
@using Microsoft.AspNetCore.Identity
@model IEnumerable<SocialGoal.Model.Models.Goal>

@if (Model.Count() == 0)
{
    <div class="alert alert-info">No goals to display</div>
}

@foreach (var item in Model.Take(10))
{
    <table class="table">
        <tbody>
            <tr>
                <td class="span1"><i class="icon-flag" /></td>
                <td class="span11">@Html.ActionLink(item.GoalName, "Index", "Goal", new { id = item.GoalId }, new { @title = item.GoalName })
                </td>
            </tr>
        </tbody>
    </table>
}

@if (Model.Count() > 10)
{
    @Html.ActionLink("See All", "GoalList", "Goal", new { filterBy = "My Goals" }, new { @style = "float:right;" })
}